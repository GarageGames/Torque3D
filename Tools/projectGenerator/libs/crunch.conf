<?php

beginLibConfig( 'crunch', '{CE252A1D-BCDE-4329-A935-3184CB104EBD}');
   // Source
   addLibSrcDir( 'crunch/inc' );
   addLibSrcDir( 'crunch/crnlib' );
   // Additional includes
   addLibIncludePath( 'crunch/inc' );
   addLibIncludePath( 'crunch/crnlib' );
   addProjectDefine( 'NDEBUG' ); // Uncomment this for release build of the library
   //addProjectDefine( '_DEBUG' ); // Use this define to get debug build of the library
   //addProjectDefine( 'CRNLIB_BUILD_DEBUG' ); // Use this define to get debug build of the library

   addProjectDefine( '_LIB' );
   if( T3D_Generator::$platform == "win32" )
   {
      addProjectDefine( 'WIN32' );
   }
   
endLibConfig();

?>